#include "main.h"
/**
 * mul - a program that multiplies two numbers
 * @a: the first number
 * @b: the second number
 * Return: the value of that mul always
 */
int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
