#include "main.h"
/**
 * add -  a program that adds two integers
 * @n: the first integer
 * @m: the second integer
 * Return: the result of the addition
 */
int add(int n, int m)
{
	int res;

	res = (n + m);
	return (res);
}
