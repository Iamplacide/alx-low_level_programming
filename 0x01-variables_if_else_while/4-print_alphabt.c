#include <stdio.h>
/**
 * main - A program that prints alphabet using putchar except q and e
 * Return: 0 always
 */
int main(void)
{
	char letter = 'a';

	while (letter <= 'z')
	{
		if (letter != 'q' && letter != 'e')
		{
			putchar(letter);
		}
		letter++;
	}
	putchar('\n');
	return (0);
}
